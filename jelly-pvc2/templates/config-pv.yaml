apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "jelly-pvc2.fullname" . }}-config-pv
  labels:
  {{- include "jelly-pvc2.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 5Gi
  hostPath:
    path: /home/vova/jellyfin/config
  storageClassName: ""