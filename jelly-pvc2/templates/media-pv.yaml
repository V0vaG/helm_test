apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "jelly-pvc2.fullname" . }}-media-pv
  labels:
  {{- include "jelly-pvc2.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 50Gi
  hostPath:
    path: /home/vova/jellyfin/media
  storageClassName: ""