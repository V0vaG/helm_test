apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "shinobi.fullname" . }}-videos
  labels:
  {{- include "shinobi.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.videos.storageRequest | quote }}
  storageClassName: {{ .Values.pvc.videos.storageClass | quote }}