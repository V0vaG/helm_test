apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "shinobi.fullname" . }}-custom
  labels:
  {{- include "shinobi.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.custom.storageRequest | quote }}