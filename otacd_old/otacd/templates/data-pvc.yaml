apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "otacd.fullname" . }}-data-pvc
  labels:
  {{- include "otacd.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.dataPvc.storageRequest | quote }}
  storageClassName: {{ .Values.pvc.dataPvc.storageClass | quote }}
  volumeName: otacd-data-pv