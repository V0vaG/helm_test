project: default
source:
  repoURL: https://github.com/V0vaG/helm_test
  path: apps
  targetRevision: main
destination:
  server: https://kubernetes.default.svc
  namespace: apps
syncPolicy:
  automated:
    prune: true
    selfHeal: true

