apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "minihub.fullname" . }}-deployment
  labels:
  {{- include "minihub.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.deployment.replicas }}
  strategy:
    rollingUpdate:
      maxSurge: {{ .Values.deployment.strategy.rollingUpdate.maxSurge | quote }}
      maxUnavailable: {{ .Values.deployment.strategy.rollingUpdate.maxUnavailable }}
    type: {{ .Values.deployment.strategy.type | quote }}
  selector:
    matchLabels:
      app: minihub
    {{- include "minihub.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: minihub
      {{- include "minihub.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - env:
        - name: KUBERNETES_CLUSTER_DOMAIN
          value: {{ quote .Values.kubernetesClusterDomain }}
        image: {{ .Values.deployment.minihub.image.repository }}:{{ .Values.deployment.minihub.image.tag
          | default .Chart.AppVersion }}
        name: minihub
        ports:
        - containerPort: 5000
        resources: {{- toYaml .Values.deployment.minihub.resources | nindent 10 }}
        volumeMounts:
        - mountPath: /root/script_files/minihub
          name: data-volume
      imagePullSecrets:
      - name: regcred
      volumes:
      - name: data-volume
        persistentVolumeClaim:
          claimName: {{ include "minihub.fullname" . }}-data-pvc