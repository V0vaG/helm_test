apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "minihub.fullname" . }}-data-pv
  labels:
  {{- include "minihub.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 1Gi
  hostPath:
    path: /home/vova/script_files/minihub
  storageClassName: ""