apiVersion: v1
kind: Namespace
metadata:
  name: minihub
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: minihub-data-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /home/vova/script_files/minihub
  storageClassName: ""
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minihub-data-pvc
  namespace: minihub
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: minihub-data-pv
  storageClassName: ""
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: minihub-deployment
  namespace: minihub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: minihub
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: minihub
    spec:
      containers:
      - name: minihub
        image: vova0911/minihub:amd64_latest
        ports:
        - containerPort: 5000
        # Uncomment if needed
        # imagePullSecrets:
        # - name: regcred
        resources:
          requests:
            memory: "256Mi"
            cpu: "500m"
          limits:
            memory: "512Mi"
            cpu: "1"
        volumeMounts:
        - name: data-volume
          mountPath: /root/script_files/minihub
      volumes:
      - name: data-volume
        persistentVolumeClaim:
          claimName: minihub-data-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: minihub-service
  namespace: minihub
spec:
  selector:
    app: minihub
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30093
  type: NodePort
