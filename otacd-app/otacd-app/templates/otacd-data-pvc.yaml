apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "otacd-app.fullname" . }}-otacd-data-pvc
  labels:
  {{- include "otacd-app.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.otacdDataPvc.storageRequest | quote }}
  storageClassName: {{ .Values.pvc.otacdDataPvc.storageClass | quote }}

