kubernetesClusterDomain: cluster.local
pvc:
  vhubDataPvc:
    storageClass: ""
    storageRequest: 1Gi
vhubDeployment:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 1
    type: RollingUpdate
  vhub:
    image:
      repository: vova0911/vhub
      tag: amd64_latest
    resources:
      limits:
        cpu: "1"
        memory: 512Mi
      requests:
        cpu: 500m
        memory: 256Mi
vhubService:
  ports:
  - nodePort: 30087
    port: 80
    protocol: TCP
    targetPort: 5000
  type: NodePort
