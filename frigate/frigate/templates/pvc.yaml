apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "frigate.fullname" . }}-pvc
  labels:
  {{- include "frigate.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.pvc.storageRequest | quote }}
  storageClassName: {{ .Values.pvc.pvc.storageClass | quote }}