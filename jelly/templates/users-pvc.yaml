apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "jelly.fullname" . }}-users-pvc
  labels:
  {{- include "jelly.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.usersPvc.storageRequest | quote }}
  volumeName: jellyfin-users-pv