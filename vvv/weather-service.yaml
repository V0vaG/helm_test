apiVersion: v1
kind: Service
metadata:
  name: weather-service
  namespace: weather
spec:
  selector:
    app: weather
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080  # This will bind to this port on your Node
  type: NodePort
