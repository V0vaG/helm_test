apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "vova-stl.fullname" . }}-deployment
  labels:
  {{- include "vova-stl.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.deployment.replicas }}
  strategy:
    rollingUpdate:
      maxSurge: {{ .Values.deployment.strategy.rollingUpdate.maxSurge | quote }}
      maxUnavailable: {{ .Values.deployment.strategy.rollingUpdate.maxUnavailable }}
    type: {{ .Values.deployment.strategy.type | quote }}
  selector:
    matchLabels:
      app: vova-stl
    {{- include "vova-stl.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: vova-stl
      {{- include "vova-stl.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - env:
        - name: KUBERNETES_CLUSTER_DOMAIN
          value: {{ quote .Values.kubernetesClusterDomain }}
        image: {{ .Values.deployment.vovaStl.image.repository }}:{{ .Values.deployment.vovaStl.image.tag
          | default .Chart.AppVersion }}
        name: vova-stl
        ports:
        - containerPort: 5000
        resources: {{- toYaml .Values.deployment.vovaStl.resources | nindent 10 }}
        volumeMounts:
        - mountPath: /root/script_files/vova-stl
          name: data-volume
      volumes:
      - name: data-volume
        persistentVolumeClaim:
          claimName: {{ include "vova-stl.fullname" . }}-data-pvc