apiVersion: v1
kind: Service
metadata:
  name: {{ include "vvolt.fullname" . }}-mongodb-service
  labels:
  {{- include "vvolt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.mongodbService.type }}
  selector:
    app: mongodb
    {{- include "vvolt.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.mongodbService.ports | toYaml | nindent 2 }}