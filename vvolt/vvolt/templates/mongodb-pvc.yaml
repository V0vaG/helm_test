apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "vvolt.fullname" . }}-mongodb-pvc
  labels:
  {{- include "vvolt.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.mongodbPvc.storageRequest | quote }}
  storageClassName: {{ .Values.pvc.mongodbPvc.storageClass | quote }}
  volumeName: mongodb-pv