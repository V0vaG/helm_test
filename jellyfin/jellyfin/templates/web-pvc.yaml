apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "jellyfin.fullname" . }}-web-pvc
  labels:
  {{- include "jellyfin.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.webPvc.storageRequest | quote }}
  storageClassName: {{ .Values.pvc.webPvc.storageClass | quote }}
  volumeName: jellyfin-web-pv