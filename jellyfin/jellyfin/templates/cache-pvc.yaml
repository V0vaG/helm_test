apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "jellyfin.fullname" . }}-cache-pvc
  labels:
  {{- include "jellyfin.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.cachePvc.storageRequest | quote }}
  storageClassName: {{ .Values.pvc.cachePvc.storageClass | quote }}
  volumeName: jellyfin-cache-pv