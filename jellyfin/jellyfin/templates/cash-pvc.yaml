apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "jellyfin.fullname" . }}-cash-pvc
  labels:
  {{- include "jellyfin.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.cashPvc.storageRequest | quote }}
  storageClassName: {{ .Values.pvc.cashPvc.storageClass | quote }}
  volumeName: jellyfin-cash-pv