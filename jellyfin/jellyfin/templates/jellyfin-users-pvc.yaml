apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "jellyfin.fullname" . }}-jellyfin-users-pvc
  labels:
  {{- include "jellyfin.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.jellyfinUsersPvc.storageRequest | quote }}
  storageClassName: {{ .Values.pvc.jellyfinUsersPvc.storageClass | quote }}
  volumeName: jellyfin-users-pv