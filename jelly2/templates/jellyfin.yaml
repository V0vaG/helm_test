apiVersion: v1
kind: Service
metadata:
  name: {{ include "jelly.fullname" . }}-jellyfin
  labels:
  {{- include "jelly.labels" . | nindent 4 }}
spec:
  type: {{ .Values.jellyfin.type }}
  selector:
    app: jellyfin
    {{- include "jelly.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.jellyfin.ports | toYaml | nindent 2 }}