apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "vpkg.fullname" . }}-webapp-deployment
  labels:
    app: webapp
  {{- include "vpkg.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.webappDeployment.replicas }}
  selector:
    matchLabels:
      app: webapp
    {{- include "vpkg.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: webapp
      {{- include "vpkg.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - env:
        - name: ENV_VAR
          value: {{ quote .Values.webappDeployment.webappContainer.env.envVar }}
        - name: KUBERNETES_CLUSTER_DOMAIN
          value: {{ quote .Values.kubernetesClusterDomain }}
        image: {{ .Values.webappDeployment.webappContainer.image.repository }}:{{ .Values.webappDeployment.webappContainer.image.tag
          | default .Chart.AppVersion }}
        name: webapp-container
        ports:
        - containerPort: 81
        resources: {{- toYaml .Values.webappDeployment.webappContainer.resources | nindent
          10 }}