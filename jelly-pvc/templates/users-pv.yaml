apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "jelly-pvc.fullname" . }}-users-pv
  labels:
  {{- include "jelly-pvc.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 5Gi
  hostPath:
    path: /home/vova/jellyfin/users
  storageClassName: ""