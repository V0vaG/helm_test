apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "artifactory.fullname" . }}-pv-artifactory-data
  labels:
    pv: pv-artifactory-data
  {{- include "artifactory.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 50Gi
  hostPath:
    path: /home/vova/script_files/artifactory/data
    type: DirectoryOrCreate
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual