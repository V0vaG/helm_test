apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ include "apps.fullname" . }}-app-of-apps
  labels:
  {{- include "apps.labels" . | nindent 4 }}
spec:
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  project: default
  source:
    path: apps
    repoURL: https://github.com/V0vaG/helm_test
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true